# Place this .envrc file in the root workspace folder

# Inject 1password secrets into environment
use_sourceop() {
  if printenv | grep -q "op://"; then
    source <(printenv | grep "op://" | op inject)
  fi
}
